# Optional override — by default Vite proxies /api and /health to localhost:8000
# (see vite.config.ts). Set VITE_API_URL if you want to hit a remote backend
# directly from the browser.
# VITE_API_URL=https://api.kensei.app
